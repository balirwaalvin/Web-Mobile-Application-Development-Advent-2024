// Storing the students' UACE points
let UACEPoints = 5; // Replacing points with the actual UACE points

// Check eligibility based on UACE points
if (UACEPoints >= 7) {
  console.log("Eligible for BSIT and Data Science");
} else {
  console.log("Choose Social Science.");
}
